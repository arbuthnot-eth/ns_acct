# Build and trace artifacts
build/*
.trace
.coverage*

# Node.js/TypeScript artifacts (now in root)
node_modules/
*.js
*.js.map
dist/

# Deployment artifacts (ignore results, keep config.json and deployment_registry.json for reference)
all_objects.json
deployment_result.json

# Generated documentation
codebase.txt
codebase.md

# Other ignores (if applicable, e.g., frontend if it exists)
frontend/node_modules